#!/bin/bash -e

# If running the rails server then create/migrate DB, then seed (optional)
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  ./bin/rails db:prepare
  if [ "${RUN_DB_SEED:-true}" = "true" ]; then
    ./bin/rails db:seed
  fi
fi

exec "${@}"
